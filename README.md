# 🛡️ Censorship-Resistant VPN Stack (Docker)

![Docker](https://img.shields.io/badge/Docker-24.0+-2496ED?style=flat&logo=docker&logoColor=white)
![Ubuntu](https://img.shields.io/badge/Ubuntu-22.04%2B-E95420?style=flat&logo=ubuntu&logoColor=white)
![Xray](https://img.shields.io/badge/Xray-Core-green)
![AmneziaWG](https://img.shields.io/badge/Amnezia-WG-blue)

Комплексное решение для развертывания личного VPN-сервера, устойчивого к блокировкам DPI (Deep Packet Inspection) и РКН. Проект автоматически настраивает три современных протокола в изолированных Docker-контейнерах.

## ✨ Возможности

1.  **VLESS + XTLS-Reality**: Самый передовой метод обхода. Маскирует трафик под посещение легитимного зарубежного сайта (например, Google или Yahoo). Не требует покупки домена.
2.  **Shadowsocks-2022**: Обновленная версия классического протокола с AEAD-шифрованием.
3.  **AmneziaWG**: Модифицированный WireGuard с обфускацией пакетов (защита от детекции по заголовкам).
4.  **Автоматизация**: Скрипт `setup.sh` сам генерирует UUID, ключи x25519, пароли и создает конфигурации.
5.  **Изоляция**: Все сервисы работают в Docker, не засоряя основную систему.
6.  **Безопасность**: Автоматическая настройка брандмауэра UFW с минимально необходимыми открытыми портами.

## 📋 Требования

*   **VPS/VDS сервер**: Ubuntu 22.04 или 24.04 (рекомендуется чистая установка).
*   **Docker**: Версия 24.0+ с поддержкой compose
*   **Ресурсы**: Минимум 1 CPU, 512 MB RAM (рекомендуется 1 GB).
*   **Порты**: 80 и 8443 должны быть свободны (не заняты Apache/Nginx).
*   **Права суперпользователя**: Скрипт требует запуска с правами root.

## 🚀 Быстрый старт

### 1. Клонирование репозитория
Зайдите на сервер по SSH и выполните:

```bash
git clone https://github.com/asvspb/grandFW.git
cd grandFW
```

### 2. Запуск установки
Запустите главный скрипт. Он установит Docker (если нет), сгенерирует ключи и запустит контейнеры.

```bash
# Устанавливаем права на выполнение файла
chmod +x setup.sh

# Запускаем установку
sudo ./setup.sh
```

После завершения работы скрипт выведет в консоль **ссылки для подключения**.

### 3. (Опционально) Настройка переменных

Если вы хотите изменить параметры по умолчанию, скопируйте шаблон конфигурации и отредактируйте его:

```bash
cp .env.template .env
nano .env  # измените параметры по необходимости
```

Основные параметры, которые можно настроить:
- `PORT_VLESS` - порт для VLESS + Reality (по умолчанию 8443)
- `PORT_SHADOWSOCKS` - порт для Shadowsocks-2022 (по умолчанию 8443)
- `PORT_AMNEZIAWG` - порт для AmneziaWG (по умолчанию 51820)
- `SERVER_NAME` - сервер для маскировки (по умолчанию google.com)

Все остальные параметры (UUID, ключи, пароли) будут автоматически сгенерированы при первом запуске, если они не заданы.
Если вы хотите изменить параметры по умолчанию, скопируйте шаблон конфигурации и отредактируйте его:

```bash
cp .env.template .env
nano .env  # измените параметры по необходимости
```

## 📱 Как подключиться

### VLESS-Reality / Shadowsocks
Используйте приложения, поддерживающие Xray/Sing-box.
*   **Android:** [Hiddify](https://github.com/hiddify/hiddify-next), [v2rayNG](https://github.com/2dust/v2rayNG).
*   **iOS:** Streisand, V2Box, Shadowrocket ($).
*   **Windows/Mac/Linux:** [Hiddify](https://github.com/hiddify/hiddify-next).

**Инструкция:**
1. После установки в корневой директории проекта будет создан файл `connection_guide.txt` с готовыми ссылками и инструкциями по подключению.
2. Скопируйте нужные ссылки из этого файла или из вывода терминала после установки.
3. Откройте приложение и нажмите "Добавить из буфера обмена".

### AmneziaWG
Обычный клиент WireGuard **не подойдет**.
*   **Все платформы:** [AmneziaVPN](https://amnezia.org/).

**Инструкция:**
1. После установки в корневой директории проекта будет создан файл `connection_guide.txt` с готовым конфигурационным файлом для AmneziaVPN.
2. Скопируйте конфигурацию из этого файла или из вывода терминала после установки.
3. В приложении выберите: `Добавить сервер` -> `Текстовый конфиг` (или файл).

## 📂 Структура проекта

```text
.
├── docker-compose.yml          # Описание сервисов (Xray, AmneziaWG)
├── setup.sh                    # Главный скрипт установки и генерации ключей
├── .env.template               # Шаблон переменных окружения
├── .env                        # Ваши секретные ключи (в git не попадает!)
├── configs/                    # Шаблоны конфигов (json, conf)
│   ├── xray.json.template
│   └── amnezia.conf.template
├── clients/                    # (Создается скриптом) Готовые файлы для клиентов
├── connection_guide.txt        # (Создается в корневой директории скриптом) Инструкция по подключению с готовыми ссылками
└── README.md
```

## 🔧 Настройка параметров

Все настройки находятся в файле `.env`. При первом запуске скрипт автоматически генерирует все необходимые параметры, но вы можете их изменить:

*   `UUID` - UUID для VLESS (генерируется автоматически, если пусто)
*   `SERVER_NAME` - сервер для маскировки (по умолчанию google.com)
*   `SNI` - SNI для TLS (обычно совпадает с SERVER_NAME)
*   `PORT_VLESS` - порт для VLESS + Reality (по умолчанию 8443)
*   `PORT_SHADOWSOCKS` - порт для Shadowsocks-2022 (по умолчанию 8443)
*   `PORT_AMNEZIAWG` - порт для AmneziaWG (по умолчанию 51820)

## 🛠 Обслуживание

**Посмотреть логи:**
```bash
docker compose logs -f
```

**Остановить сервер:**
```bash
docker compose down
```

**Удалить скаченные образы Docker:**

Для удаления всех неиспользуемых образов:
```bash
docker image prune -a
```

Для удаления всех неиспользуемых ресурсов Docker (контейнеры, сети, образы, кэш):
```bash
docker system prune -a
```

Для удаления конкретных образов проекта:
```bash
docker rmi teddysun/xray ghcr.io/linuxserver/wireguard:latest
```

Для просмотра используемого пространства:
```bash
docker system df
```

```bash
docker compose down
```

**Перезапустить (применить изменения конфига):**
```bash
docker compose restart
```

**Обновить образы:**
```bash
docker compose pull && docker compose up -d
```

**Повторный запуск скрипта установки:**
```bash
sudo ./setup.sh
```
Скрипт идемпотентен, поэтому его можно запускать несколько раз без риска повредить существующую конфигурацию.

**Получить снова информацию для подключения:**
```bash
./setup.sh --info
```

**Обновить файл с инструкциями подключения:**
Файл `connection_guide.txt` можно заново сгенерировать командой:
```bash
./setup.sh --info
```
```bash
./setup.sh --info
```

## 🔧 Устранение неполадок

### Проблемы с образами

Если официальный образ AmneziaWG недоступен (ошибка `denied`/`access denied`), используется публичный образ WireGuard `ghcr.io/linuxserver/wireguard:latest`, который настроен для работы с параметрами AmneziaWG.

Если возникают ошибки с sysctl параметрами (например, `net.core.rmem_max not allowed in host network namespace`), используется сетевой режим по умолчанию (bridge) с публикацией портов вместо host network mode.

Обратите внимание, что в связи с особенностями образов:
- Не используется network_mode: host для обеспечения совместимости
- Порты публикуются с помощью директивы ports в docker-compose.yml
- Для работы контейнеров требуются соответствующие разрешения (cap_add)
- Порт для AmneziaWG теперь публикуется через docker-compose.yml и не требует отдельного разрешения в UFW

Если возникают ошибки с sysctl параметрами (например, `net.core.rmem_max not allowed in host network namespace`), используется сетевой режим по умолчанию (bridge) с публикацией портов вместо host network mode.

Обратите внимание, что в связи с особенностями образов:
- Не используется network_mode: host для обеспечения совместимости
- Порты публикуются с помощью директивы ports в docker-compose.yml
- Для работы контейнеров требуются соответствующие разрешения (cap_add)

Обратите внимание, что в связи с особенностями образа linuxserver/wireguard:
- Не используется network_mode: host
- Не применяются специфические sysctls для AmneziaWG
- Конфигурационный файл монтируется в /config/wg0.conf
- Для работы контейнера требуются переменные PUID, PGID и TZ

Если после клонирования репозитория возникает ошибка при запуске скрипта:
```
Permission denied
```
или
```
Command './setup.sh' not found
```

Выполните команду для установки прав на выполнение:
```bash
chmod +x setup.sh
```

Затем запустите установку:
```bash
sudo ./setup.sh
```

Если во время установки возникает ошибка "Порт 8443 уже занят другим приложением", выполните одно из следующих действий:

Если во время установки возникает ошибка "Порт 8443 уже занят другим приложением", выполните одно из следующих действий:

1. Остановите веб-сервер, использующий порт 8443:
   ```bash
   sudo systemctl stop nginx  # или apache2
   sudo systemctl disable nginx  # или apache2, чтобы не запускался при перезагрузке
   ```

2. Или измените порт в файле `.env`, изменив значение `PORT_VLESS` на другой (например, 2053 или 2083), и перезапустите установку.
```bash
./setup.sh --info
```

## ⚠️ Безопасность

*   Файл `.env` содержит приватные ключи. **Никогда не публикуйте его в публичных репозиториях.**
*   Порт 8443 используется для маскировки. Не занимайте его веб-серверами (Nginx/Apache), либо настройте fallback внутри Xray.
*   Если на сервере уже запущен веб-сервер на порту 8443, остановите его или измените настройки в `.env` файле, чтобы использовать другой порт.
*   При использовании bridge network mode (вместо host) порты публикуются в соответствии с настройками в docker-compose.yml, а не требуют отдельного открытия в UFW.
*   Скрипт автоматически настраивает брандмауэр UFW, разрешая только необходимые порты.
*   При использовании bridge network mode (вместо host) порты публикуются в соответствии с настройками в docker-compose.yml.
*   Все пароли и ключи генерируются автоматически с криптографической стойкостью.

## 🛡️ Проверка и безопасность

*   Скрипт проверяет занятость порта 8443 перед установкой, чтобы избежать конфликта с веб-серверами
*   Для AmneziaWG (реализованный через образ linuxserver/wireguard) добавлены необходимые права (SYS_MODULE) для корректной работы на низком уровне
*   Реализована автоматическая проверка кода через GitHub Actions (ShellCheck)

## 🔄 Особенности реализации

*   **Идемпотентность**: Скрипт можно запускать многократно, он проверяет текущее состояние и обновляет конфигурацию при необходимости.
*   **Безопасность**: Все ключи и пароли генерируются с помощью `openssl` с криптографической стойкостью.
*   **Конфигурация**: Все параметры вынесены в файл `.env`, что позволяет легко настраивать конфигурацию без изменения кода.
*   **Логирование**: Скрипт выводит подробные сообщения о каждом этапе установки.
*   **Управление контейнерами**: Используется `docker compose` для управления контейнерами.

## 📄 Лицензия

MIT License
