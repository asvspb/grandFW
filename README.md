# 🛡️ Censorship-Resistant VPN Stack (Docker)

![Docker](https://img.shields.io/badge/Docker-24.0+-2496ED?style=flat&logo=docker&logoColor=white)
![Ubuntu](https://img.shields.io/badge/Ubuntu-22.04%2B-E95420?style=flat&logo=ubuntu&logoColor=white)
![Xray](https://img.shields.io/badge/Xray-Core-green)
![AmneziaWG](https://img.shields.io/badge/Amnezia-WG-blue)

Комплексное решение для развертывания личного VPN-сервера, устойчивого к блокировкам DPI (Deep Packet Inspection) и РКН. Проект автоматически настраивает три современных протокола в изолированных Docker-контейнерах.

## ✨ Возможности

1.  **VLESS + XTLS-Reality**: Самый передовой метод обхода. Маскирует трафик под посещение легитимного зарубежного сайта (например, Microsoft или Yahoo). Не требует покупки домена.
2.  **Shadowsocks-2022**: Обновленная версия классического протокола с AEAD-шифрованием.
3.  **AmneziaWG**: Модифицированный WireGuard с обфускацией пакетов (защита от детекции по заголовкам).
4.  **Автоматизация**: Скрипт `setup.sh` сам генерирует UUID, ключи x25519, пароли и создает конфигурации.
5.  **Изоляция**: Все сервисы работают в Docker, не засоряя основную систему.
6.  **Безопасность**: Автоматическая настройка брандмауэра UFW с минимально необходимыми открытыми портами.

## 📋 Требования

*   **VPS/VDS сервер**: Ubuntu 22.04 или 24.04 (рекомендуется чистая установка).
*   **Docker**: Версия 24.0+ с поддержкой compose
*   **Ресурсы**: Минимум 1 CPU, 512 MB RAM (рекомендуется 1 GB).
*   **Порты**: 80 и 443 должны быть свободны (не заняты Apache/Nginx).
*   **Права суперпользователя**: Скрипт требует запуска с правами root.

## 🚀 Быстрый старт

### 1. Клонирование репозитория
Зайдите на сервер по SSH и выполните:

```bash
git clone https://github.com/asvspb/grandFW.git
cd grandFW
```

### 2. Запуск установки
Запустите главный скрипт. Он установит Docker (если нет), сгенерирует ключи и запустит контейнеры.

```bash
chmod +x setup.sh
sudo ./setup.sh
```

После завершения работы скрипт выведет в консоль **ссылки для подключения**.

### 3. (Опционально) Настройка переменных
Если вы хотите изменить параметры по умолчанию, скопируйте шаблон конфигурации и отредактируйте его:

```bash
cp .env.template .env
nano .env  # измените параметры по необходимости
```

## 📱 Как подключиться

### VLESS-Reality / Shadowsocks
Используйте приложения, поддерживающие Xray/Sing-box.
*   **Android:** [Hiddify](https://github.com/hiddify/hiddify-next), [v2rayNG](https://github.com/2dust/v2rayNG).
*   **iOS:** Streisand, V2Box, Shadowrocket ($).
*   **Windows/Mac/Linux:** [Hiddify](https://github.com/hiddify/hiddify-next).

**Инструкция:**
1. Скопируйте ссылку `vless://...` или `ss://...` из вывода терминала после установки.
2. Откройте приложение и нажмите "Добавить из буфера обмена".

### AmneziaWG
Обычный клиент WireGuard **не подойдет**.
*   **Все платформы:** [AmneziaVPN](https://amnezia.org/).

**Инструкция:**
1. Скопируйте блок конфигурации, который выдал скрипт.
2. В приложении выберите: `Добавить сервер` -> `Текстовый конфиг` (или файл).

## 📂 Структура проекта

```text
.
├── docker-compose.yml      # Описание сервисов (Xray, AmneziaWG)
├── setup.sh                # Главный скрипт установки и генерации ключей
├── .env.template           # Шаблон переменных окружения
├── .env                    # Ваши секретные ключи (в git не попадает!)
├── configs/                # Шаблоны конфигов (json, conf)
│   ├── xray.json.template
│   └── amnezia.conf.template
├── clients/                # (Создается скриптом) Готовые файлы для клиентов
└── README.md
```

## 🔧 Настройка параметров

Все настройки находятся в файле `.env`. При первом запуске скрипт автоматически генерирует все необходимые параметры, но вы можете их изменить:

*   `UUID` - UUID для VLESS (генерируется автоматически, если пусто)
*   `SERVER_NAME` - сервер для маскировки (по умолчанию www.microsoft.com)
*   `SNI` - SNI для TLS (обычно совпадает с SERVER_NAME)
*   `PORT_VLESS` - порт для VLESS + Reality (по умолчанию 443)
*   `PORT_SHADOWSOCKS` - порт для Shadowsocks-2022 (по умолчанию 8443)
*   `PORT_AMNEZIAWG` - порт для AmneziaWG (по умолчанию 51820)

## 🛠 Обслуживание

**Посмотреть логи:**
```bash
docker compose logs -f
```

**Остановить сервер:**
```bash
docker compose down
```

**Перезапустить (применить изменения конфига):**
```bash
docker compose restart
```

**Обновить образы:**
```bash
docker compose pull && docker compose up -d
```

**Повторный запуск скрипта установки:**
```bash
sudo ./setup.sh
```
Скрипт идемпотентен, поэтому его можно запускать несколько раз без риска повредить существующую конфигурацию.

**Получить снова информацию для подключения:**
```bash
./setup.sh --info
```

## ⚠️ Безопасность

*   Файл `.env` содержит приватные ключи. **Никогда не публикуйте его в публичных репозиториях.**
*   Порт 443 используется для маскировки. Не занимайте его веб-серверами (Nginx/Apache), либо настройте fallback внутри Xray.
*   Скрипт автоматически настраивает брандмауэр UFW, разрешая только необходимые порты.
*   Все пароли и ключи генерируются автоматически с криптографической стойкостью.

## 🛡️ Проверка и безопасность

*   Скрипт проверяет занятость порта 443 перед установкой, чтобы избежать конфликта с веб-серверами
*   Для AmneziaWG добавлены необходимые права (SYS_MODULE) для корректной работы на низком уровне
*   Реализована автоматическая проверка кода через GitHub Actions (ShellCheck)

## 🔄 Особенности реализации

*   **Идемпотентность**: Скрипт можно запускать многократно, он проверяет текущее состояние и обновляет конфигурацию при необходимости.
*   **Безопасность**: Все ключи и пароли генерируются с помощью `openssl` с криптографической стойкостью.
*   **Конфигурация**: Все параметры вынесены в файл `.env`, что позволяет легко настраивать конфигурацию без изменения кода.
*   **Логирование**: Скрипт выводит подробные сообщения о каждом этапе установки.
*   **Управление контейнерами**: Используется `docker compose` для управления контейнерами.

## 📄 Лицензия

MIT License